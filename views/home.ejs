<%- include('partials/header') %>
<h1>Home</h1>

<p><%=text%></p>

<% posts.forEach(item => { %>
    <h3><%=item.title%></h3>
    <% console.log(item.title); %>
    <p><%=item.info%></p>
<%})%>
<%- include('partials/footer') %>